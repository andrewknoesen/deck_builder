# Not a uv project — `backend/` is a standalone uv project with its own
# pyproject.toml, uv.lock, and .venv. This file exists only so editors opened
# at the repo root (rather than in backend/) can resolve backend's modules.
[tool.pyright]
extraPaths = ["backend"]

[tool.mypy]
mypy_path = "backend"
